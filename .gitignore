.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
DerivedData/
.DS_Store
__pycache__/
build/

# Regenerated by Scripts/make-icons.sh on the way to AppIcon.icns.
Assets/AppIcon.iconset/

# The custom folder icon Finder writes. Its name ends in a carriage return,
# and matching that exactly matters: git on a Mac ignores case, so a looser
# Icon? also caught chrome/icons and kept the extension's icons out. The
# line below is Icon and two carriage returns, since git strips one as a line
# ending.
Icon

# Note-to-self, not part of the app.
latex-unicode-app-build-prompt.md
ARCHITECTURE.md

# Windows port build output
windows/**/bin/
windows/**/obj/
windows/dist/
windows/LaTeXSquiggly.App/Assets/preview/
